{
  "name": "dbpcms",
  "version": "0.1.0",
  "private": true,
  "description": "Donna Barbar Polytechnic College Management System (DBPCMS)",
  "engines": {
    "node": ">=22 <25"
  },
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "dev": "pnpm --filter @dbpcms/shared build && pnpm --parallel --filter \"./apps/*\" dev",
    "setup": "pnpm install && pnpm --filter @dbpcms/shared build && pnpm --filter @dbpcms/api prisma:generate",
    "dev:api": "pnpm --filter @dbpcms/api dev",
    "dev:web": "pnpm --filter @dbpcms/web dev",
    "build": "pnpm --filter @dbpcms/shared build && pnpm --filter @dbpcms/api build && pnpm --filter @dbpcms/web build",
    "typecheck": "pnpm --recursive typecheck",
    "lint": "pnpm --recursive lint",
    "test": "pnpm --recursive test",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "argon2",
      "prisma",
      "@prisma/client",
      "@prisma/engines"
    ]
  }
}
